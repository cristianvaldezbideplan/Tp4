<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ejercisiotp4">

    <application
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:allowBackup="true"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ejercisiotp4">
        <activity android:name=".Activity_Cuenta_Configured"></activity>
        <activity android:name=".Activity_InicioSesion" />
        <activity android:name=".Activity_Registrar" />
        <activity android:name=".Activity_Login" />
        <activity android:name=".Activity_principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>